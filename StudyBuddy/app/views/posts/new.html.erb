<h1> Create Post </h1>

<%= form_for :post, url: posts_path do |f| %>
	<p> 
		<%= f.label :course %> <br>
		<%= f.text_field :course %>
	</p>
	<p> 
		<%= f.label :content %> <br>
		<%= f.text_area :content %>
	</p>

	<p>
		<%= f.submit %>
	</p>

<% end %>

<%= link_to 'Back to Posts', posts_path %>